// URL base del backend — cambia solo qui per passare da sviluppo a produzione
// Usa var (non const/let) per esporre come window.API_BASE ai moduli ES
var API_BASE = 'http://127.0.0.1:8080';

// Google Maps JavaScript API key — var necessario per window.GOOGLE_MAPS_KEY nei moduli ES
var GOOGLE_MAPS_KEY = 'INSERISCI_LA_TUA_CHIAVE_GOOGLE_MAPS';
